# Taskflow--AI
A full-stack Task Manager application built using the MERN stack (MongoDB, Express.js, React.js, and Node.js). It features user authentication with JWT, secure password hashing using bcrypt, CRUD operations for task management, RESTful APIs, responsive UI, and Docker-based deployment for scalable and efficient application management.
